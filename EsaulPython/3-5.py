# Инициализация переменной для хранения суммы
sum_of_numbers = 0

# Цикл для чтения 10 чисел и добавления их к сумме
for _ in range(10):
    sum_of_numbers += int(input())

# Вывод суммы
print(sum_of_numbers)
